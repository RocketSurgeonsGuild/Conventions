//------------------------------------------------------------------------------
// <auto-generated>
//     Generated from ClavusPart.cs.template for the '{PartName}' part.
//     Changes to this file will be lost when the file is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#pragma warning disable CS0105, CA1002, CA1034, CA1822, CS8603, CS8602, CS8618
using System;
using System.Diagnostics;
using System.Reflection;
using System.Runtime.CompilerServices;
using System.Threading;
using System.Threading.Tasks;
using Clavus;
using Clavus.Infrastructure;
using PropertiesDictionary = System.Collections.Generic.Dictionary<object, object>;
using PropertiesType = System.Collections.Generic.IDictionary<object, object>;

namespace {Namespace};

/// <summary>
/// Extension methods for configuring Clavus on the {HostBuilderType} host builder.
/// </summary>
[System.CodeDom.Compiler.GeneratedCode("Clavus", "generated"), System.Runtime.CompilerServices.CompilerGenerated, System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
internal static partial class GeneratedClavus{HostName}Extensions
{
    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static ValueTask<{HostType}> ConfigureClavus(this {HostBuilderType} builder, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var contextBuilder = ClavusContextBuilder.Create();
        return ConfigureClavus(builder, contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this Task<{HostBuilderType}> builder, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        var contextBuilder = ClavusContextBuilder.Create();
        return await ConfigureClavus(await builder, contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="action">An action to configure the Clavus context builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this {HostBuilderType} builder, Func<ClavusContextBuilder, CancellationToken, ValueTask> action, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        ArgumentNullException.ThrowIfNull(action);
        var contextBuilder = ClavusContextBuilder.Create();
        await action(contextBuilder, cancellationToken);
        return await ConfigureClavus(builder, contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="action">An action to configure the Clavus context builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this Task<{HostBuilderType}> builder, Func<ClavusContextBuilder, CancellationToken, ValueTask> action, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        ArgumentNullException.ThrowIfNull(action);
        var contextBuilder = ClavusContextBuilder.Create();
        await action(contextBuilder, cancellationToken);
        return await ConfigureClavus(await builder, contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="action">An action to configure the Clavus context builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this {HostBuilderType} builder, Func<ClavusContextBuilder, ValueTask> action, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        ArgumentNullException.ThrowIfNull(action);
        var contextBuilder = ClavusContextBuilder.Create();
        await action(contextBuilder);
        return await ConfigureClavus(builder, contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="action">An action to configure the Clavus context builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this Task<{HostBuilderType}> builder, Func<ClavusContextBuilder, ValueTask> action, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        ArgumentNullException.ThrowIfNull(action);
        var contextBuilder = ClavusContextBuilder.Create();
        await action(contextBuilder);
        return await ConfigureClavus(await builder, contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="action">An action to configure the Clavus context builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static ValueTask<{HostType}> ConfigureClavus(this {HostBuilderType} builder, Action<ClavusContextBuilder> action, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        ArgumentNullException.ThrowIfNull(action);
        var contextBuilder = ClavusContextBuilder.Create();
        action(contextBuilder);
        return ConfigureClavus(builder, contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="action">An action to configure the Clavus context builder.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this Task<{HostBuilderType}> builder, Action<ClavusContextBuilder> action, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        ArgumentNullException.ThrowIfNull(action);
        var contextBuilder = ClavusContextBuilder.Create();
        action(contextBuilder);
        return await ConfigureClavus(await builder, action, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder.</param>
    /// <param name="contextBuilder">The <see cref="ClavusContextBuilder"/>.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this {HostBuilderType} builder, ClavusContextBuilder contextBuilder, CancellationToken cancellationToken = default)
    {
        ArgumentNullException.ThrowIfNull(builder);
        ArgumentNullException.ThrowIfNull(contextBuilder);
        return await global::Clavus.Hosting.RocketHostApplicationExtensions.Configure(builder, static b => b.Build(), contextBuilder, cancellationToken);
    }

    /// <summary>
    /// Configures Clavus on the {HostBuilderType} host builder.
    /// </summary>
    /// <param name="builder">The {HostBuilderType} host builder (as a task).</param>
    /// <param name="contextBuilder">The <see cref="ClavusContextBuilder"/>.</param>
    /// <param name="cancellationToken">A cancellation token.</param>
    public static async ValueTask<{HostType}> ConfigureClavus(this Task<{HostBuilderType}> builder, ClavusContextBuilder contextBuilder, CancellationToken cancellationToken = default)
        => await ConfigureClavus(await builder, contextBuilder, cancellationToken);
}
#pragma warning restore CS0105, CA1002, CA1034, CA1822, CS8603, CS8602, CS8618
