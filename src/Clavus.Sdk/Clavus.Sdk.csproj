<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <NoWarn>$(NoWarn);NU5128</NoWarn>
    <PackageType>MSBuildSdk</PackageType>
    <NoBuildOutput>true</NoBuildOutput>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <IncludeSymbols>false</IncludeSymbols>
    <ImplicitUsings>disable</ImplicitUsings>
    <PackageDescription>MSBuild SDK for Clavus — auto-selects packages and emits bootstrapping based on host SDK</PackageDescription>
    <PackageTags>$(PackageTags);sdk;msbuild</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <Compile Remove="Sdk/**" />
    <None Include="Sdk/**" Pack="true" PackagePath="Sdk/" />
    <Using Remove="**" />
  </ItemGroup>

  <UsingTask
    TaskName="GenerateClavusPackagesProps"
    TaskFactory="RoslynCodeTaskFactory"
    AssemblyFile="$(MSBuildToolsPath)\Microsoft.Build.Tasks.Core.dll"
  >
    <ParameterGroup>
      <DestinationFile ParameterType="System.String" Required="true" />
    </ParameterGroup>
    <Task>
      <Using Namespace="System.Collections.Generic" />
      <Using Namespace="System.IO" />
      <Using Namespace="System.Linq" />
      <Using Namespace="System.Text" />
      <Code Type="Fragment" Language="cs"><![CDATA[
        var versionsById = new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase);
        var packages = Directory.EnumerateDirectories("$(MSBuildThisFileDirectory)../");
        foreach (var package in packages.Where(z => !z.Contains("Analyzers")))
        {
          versionsById[Path.GetFileName(package)] = "$(Version)";
        }

        // Built via concatenation (not a literal "$(" substring) so MSBuild's own property
        // expansion doesn't try to evaluate it while parsing this inline task's source text.
        var dollar = "$";

        var content = new StringBuilder();
        content.AppendLine("<Project>");
        content.AppendLine("    <!--");
        content.AppendLine("        Generated from Directory.Packages.props at pack time — do not edit directly.");
        content.AppendLine("        Each property is only set when the consumer has not already defined it, so any");
        content.AppendLine("        of these can be overridden from Directory.Build.props, the project file, or the");
        content.AppendLine("        command line.");
        content.AppendLine("    -->");
        content.AppendLine("    <PropertyGroup>");
        foreach (var kv in versionsById)
        {
            var propertyName = kv.Key.Replace(".", "") + "Version";
            var reference = dollar + "(" + propertyName + ")";
            content.AppendLine($"        <{propertyName} Condition=\"'{reference}' == ''\">{kv.Value}</{propertyName}>");
        }
        content.AppendLine("    </PropertyGroup>");
        content.AppendLine("</Project>");

        Directory.CreateDirectory(Path.GetDirectoryName(DestinationFile));
        File.WriteAllText(DestinationFile, content.ToString());
      ]]></Code>
    </Task>
  </UsingTask>

  <Target Name="_ClavusGeneratePackagesProps" BeforeTargets="_GetPackageFiles;Pack">
    <ItemGroup>
      <_ClavusPackagesPropsPrefix Include="Clavus" />
    </ItemGroup>

    <GenerateClavusPackagesProps       DestinationFile="$(IntermediateOutputPath)Packages.props"    />

    <ItemGroup>
      <None Include="$(IntermediateOutputPath)Packages.props" Pack="true" PackagePath="Sdk" />
    </ItemGroup>
  </Target>

</Project>
