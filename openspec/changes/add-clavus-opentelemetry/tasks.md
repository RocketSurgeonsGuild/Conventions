## 1. Project scaffolding

- [x] 1.1 Create `src/Clavus.OpenTelemetry/Clavus.OpenTelemetry.csproj` (TargetFramework `net10.0`, `RootNamespace=Clavus`, `IsTrimmable`). Note: `Clavus.Serilog`/`Clavus.DryIoc` ship **no** `build`/`buildTransitive`/`buildMultiTargeting` asset folders — part discovery is pure `<ClavusPart>` + `[ClavusExport]`, no manual MSBuild wiring needed. Task 3.5 below is adjusted accordingly.
- [x] 1.2 `OpenTelemetry`/`OpenTelemetry.Extensions.Hosting` already pinned at `1.16.0` in `Directory.Packages.props`; `PackageReference`s added. Namespaces verified via successful build: `OpenTelemetry.IOpenTelemetryBuilder`, `OpenTelemetry.Trace.TracerProviderBuilder`, `OpenTelemetry.Metrics.MeterProviderBuilder`, `OpenTelemetry.Logs.LoggerProviderBuilder`, `OpenTelemetry.Resources.ResourceBuilder` all resolve at 1.16.0 (Open Question 3 resolved).
- [x] 1.3 Project already present in `Clavus.slnx` (`/src/` folder).

## 2. Part declarations

- [x] 2.1 All five `<ClavusPart>` items present in the csproj with correct `ParameterType`/`ParameterName`, `Namespace="Clavus"`.
- [x] 2.2 Build confirmed: `obj/Debug/net10.0/ClavusPart.{OpenTelemetry,TracerProvider,MeterProvider,LoggerProvider,ResourceBuilder}.g.cs` all generated successfully.

## 3. Runtime integration

- [x] 3.1 Implement `RocketOpenTelemetryExtensions.UseOpenTelemetry(this ClavusContextBuilder)` that registers the internal integration (a `Service` part), mirroring `Clavus.DryIoc.UseDryIoc`
- [x] 3.2 In the integration, call `services.AddOpenTelemetry()` once and apply the top-level `IOpenTelemetryPart`s to the `IOpenTelemetryBuilder`
- [x] 3.3 Implement the async-apply-per-kind bridge (design Decision 2a): capture each builder inside `ConfigureResource`/`WithTracing`/`WithMetrics`/`WithLogging`, then `await` the generated `Apply{Kind}` against the captured builder in the async host-build phase — no sync-over-async inside callbacks
- [x] 3.4 Enforce apply order: `OpenTelemetry` → `ResourceBuilder` → `TracerProvider` → `MeterProvider` → `LoggerProvider`, preserving executor part priority within each kind (spec: opentelemetry-parts ordering)
- [x] 3.5 N/A — superseded by the `Clavus` package's own `build`/`buildTransitive`/`buildMultiTargeting` assets (`src/Clavus/build/Clavus.targets` → `Clavus.Parts.targets`), which every `Clavus`-referencing project already pulls in transitively; verified `Clavus.Serilog`/`Clavus.DryIoc` ship no per-package asset folders and `Clavus.OpenTelemetry.csproj` needs none either — it only declares `<ClavusPart>` items and references `Clavus`.
- [x] 3.6 Investigated moving the `OpenTelemetry.Extensions.Hosting` `PackageReference` from `Clavus.OpenTelemetry.csproj` into `Clavus.Hosting.csproj` (requested follow-up). **Decision: kept as-is, no move.** Verified (a) `OpenTelemetry.IOpenTelemetryBuilder` (the five `<ClavusPart>` `ParameterType`s) resolves from the base `OpenTelemetry` package alone — confirmed via an isolated build referencing only `OpenTelemetry` 1.16.0 — so it needs no reference to `.Extensions.Hosting` at all; (b) `services.AddOpenTelemetry()` (the one call in `RocketOpenTelemetryExtensions.UseOpenTelemetry`) is the sole thing that requires `OpenTelemetry.Extensions.Hosting`, and it's called directly inside `Clavus.OpenTelemetry`, not through any `Clavus.Hosting` hook — `UseOpenTelemetry()` uses `ClavusContextBuilder.ConfigureServices` from `Clavus` core and never references `Clavus.Hosting`; (c) `Clavus.Hosting.csproj` has zero third-party runtime deps beyond `Microsoft.Extensions.Hosting`, and is a transitive dependency of `Clavus.CommandLine`, `Clavus.Hosting.Web`, `Clavus.Hosting.Maui`, and `Clavus.Hosting.WebAssembly` — none of which touch OpenTelemetry. Moving the package reference there would force OTel assemblies onto all four, violating the opt-in design (see design.md Non-Goals / Decision 3's rejected alternative). No source or `.csproj` changes made; both projects rebuilt clean (0 errors) to confirm the existing shape is correct as-is. See `.squad/decisions/inbox/dallas-opentelemetry-hosting-package-boundary.md`.

## 4. Runtime behavioral tests (Conventions repo)

- [x] 4.1 Create `Clavus.OpenTelemetry.Tests` project referencing the OpenTelemetry in-memory exporter and the test host helpers. Mirrors `Clavus.DryIoc.Tests` conventions. `OpenTelemetry.Exporter.InMemory` pinned at `1.16.0` in `Directory.Packages.props` (matches the already-pinned `OpenTelemetry`/`OpenTelemetry.Extensions.Hosting` version). Added to `Clavus.slnx`. Builds clean.
- [x] 4.2 Test: without `UseOpenTelemetry`, `AddOpenTelemetry` is not called and no parts apply — real test, passing (`Without_UseOpenTelemetry_AddOpenTelemetry_Is_Not_Called`)
- [x] 4.3 Test: a sync `ITracerProviderPart` adds an activity source; in-memory exporter captures a span from it — real test, passing (`Sync_TracerProviderPart_Adds_ActivitySource_And_InMemoryExporter_Captures_Span`)
- [x] 4.4 Test: an `ITracerProviderAsyncPart` performs awaited work then adds a source; effect present, host build does not block/deadlock (spec: async without blocking) — real test, passing (`Async_TracerProviderPart_Performs_Awaited_Work_Then_Adds_Source_Without_Deadlock`)
- [x] 4.5 Test: `IMeterProviderPart` (metric), `ILoggerProviderPart` (log record), and `IResourceBuilderPart` (resource attribute) each verified via in-memory exporter — all three real and passing. The resource-attribute test (`ResourceBuilderPart_Adds_Attribute_Present_On_Built_Providers`) initially failed on a genuine bug found by Ash: `IOpenTelemetryBuilder.ConfigureResource`'s callback does not run synchronously in OTel 1.16.0 (unlike `WithTracing`/`WithMetrics`/`WithLogging`), so the original capture-the-builder bridge captured a null `ResourceBuilder` for this one kind. Fixed in `RocketOpenTelemetryExtensions.UseOpenTelemetry` (Dallas): build a private `ResourceBuilder.CreateEmpty()`, apply `IResourceBuilderPart`/Async parts against it with a real `await`, `Build()` it, then merge the resulting `Resource`'s attributes into the real builder via a `ConfigureResource` callback that only closes over an already-computed value (timing-independent). `dotnet test test/Clavus.OpenTelemetry.Tests/Clavus.OpenTelemetry.Tests.csproj` now 8/8 green. See `.squad/decisions/inbox/ash-clavus-opentelemetry-tests.md` (bug report) and `.squad/decisions/inbox/dallas-resource-builder-deferred-configure-fix.md` (fix).
- [x] 4.6 Test: two parts of the same kind with different priorities configure the builder in ascending priority order — real test, passing (`Two_Parts_Same_Kind_Different_Priorities_Apply_In_Ascending_Priority_Order`)
- [x] 4.7 Test: `[ClavusExport]`-annotated tracer part is auto-discovered and applied without manual `Configure` — real test, passing (`ClavusExport_Annotated_TracerPart_Is_Auto_Discovered_Without_Manual_Configure`)

## 5. Composition tests (DryIoc / Autofac / Serilog)

- [x] 5.1 Test: telemetry parts apply and emit under `UseDryIoc` + `UseOpenTelemetry` (`TelemetryParts_Apply_And_Emit_Under_UseDryIoc` in `test/Clavus.OpenTelemetry.Tests/OpenTelemetryCompositionTests.cs`, passing)
- [x] 5.2 Test: telemetry parts apply and emit under `UseAutofac` + `UseOpenTelemetry` (`TelemetryParts_Apply_And_Emit_Under_UseAutofac`, passing)
- [x] 5.3 Test: telemetry parts and the Serilog part both apply when used together, no conflict (`TelemetryParts_And_SerilogPart_Apply_Together_On_Same_Host_Without_Conflict`, passing). Note: `ApplySerilog` has no built-in host entry point analogous to `UseOpenTelemetry()` yet (confirmed no call site exists anywhere in `src/`) — the test drives it manually via an async `ConfigureServices` Service part on the same `IClavusContext`, the same mechanism `UseOpenTelemetry()` itself uses. See decision entry for detail; not a bug in this change's scope, just a gap flagged for whoever wires Serilog's runtime logging pipeline into the host.

## 6. SDK packaging & configuration-mode tests (rsg-sdk SdkTestProject)

- [x] 6.1 Packed `Clavus.OpenTelemetry` into `artifacts/nuget/` (the local feed consumed by `SdkTestProject`) via the real pipeline mechanism (`Rocket.Surgery.ModularPipelines.Extensions`'s `PackSolution` module, invoked through `dotnet run build/Build.cs`, same as every other integration package — not a bespoke `dotnet pack`). Produced `Clavus.OpenTelemetry.15.0.1-clavus.1.nupkg`/`.snupkg`, versioned identically to sibling packages via `Rocket.Surgery.MSBuild.GitVersion`. Verified the nuspec: no `build`/`buildTransitive`/`buildMultiTargeting` entries (only `lib/net10.0/`), consistent with task 3.5's N/A finding; dependency group is `Clavus` + `OpenTelemetry` + `OpenTelemetry.Extensions.Hosting` (no `Clavus.Hosting`), consistent with Dallas's "no move" decision (`dallas-opentelemetry-hosting-package-boundary.md`). Zero warnings/errors attributed to `Clavus.OpenTelemetry.csproj` in the pipeline log (the pipeline's overall exit code 1 came from a pre-existing, unrelated `NU5017` on `Clavus.Sdk.csproj`, after `Clavus.OpenTelemetry`'s pack had already succeeded). See `.squad/decisions/inbox/parker-opentelemetry-pack-6.1.md`.
- [ ] 6.2 Test: a `SdkTestProject` consumer referencing `Clavus.OpenTelemetry` + `Clavus.Sdk` restores, compiles, and packs (Verify snapshot of evaluated properties/PackageReferences)
- [ ] 6.3 Test (library-based config): telemetry/appsettings config discovered from a referenced package's `build` assets is present in the consumer evaluation
- [ ] 6.4 Test (project-reference-based config): the same config discovered via a `<ProjectReference>` is present, and the two modes produce equivalent discovered configuration
- [ ] 6.5 Test: a consumer combining `Clavus.OpenTelemetry` with DryIoc, Autofac, and Serilog packages builds and packs cleanly

## 7. Docs & finalize

- [x] 7.1 Add a short README/section for `Clavus.OpenTelemetry` (opt-in, the five part kinds, an example part)
- [ ] 7.2 Run `openspec validate --change add-clavus-opentelemetry` and the full solution build/test; resolve design Open Questions 1–2 with the chosen implementation
